<Window x:Class="PasswordManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <!--Adding account form-->
                <Border Grid.Column="0" Background="GhostWhite" BorderBrush="Gainsboro" BorderThickness="1">
                    <StackPanel Background="LightGray" Margin="4">
                        <Label HorizontalAlignment="Center" FontSize="16">Name</Label>
                        <TextBox Margin="4" Text="{Binding Name, Mode=TwoWay}"></TextBox>
                        <Label HorizontalAlignment="Center" FontSize="16">Login</Label>
                        <TextBox Margin="4" Text="{Binding Login, Mode=TwoWay}"></TextBox>
                        <Label HorizontalAlignment="Center" FontSize="16">Password</Label>
                        <TextBox Margin="4" Text="{Binding Password, Mode=TwoWay}"></TextBox>

                        <Button Height="20" Margin="5" FontSize="14" Click="Button_Click">Add</Button>
                    </StackPanel>
                </Border>
                
                <!--Generator form-->
                <Border Grid.Column="1" Background="GhostWhite" BorderBrush="Gainsboro" BorderThickness="1">
                    <StackPanel Background="LightGray" Margin="4">
                        <Label HorizontalAlignment="Center" FontSize="18">Generator</Label>
                        <TextBox IsReadOnly="True" Text="{Binding GeneratedPassword}" TextAlignment="Center" Background="White" Margin="4"/>
                        <Button FontSize="14" Margin="10" Click="Button_Click_1">Generate</Button>
                    </StackPanel>
                </Border>

            </Grid>
            
            <!--Accounts list-->
            <Border Grid.Row="1" Background="GhostWhite" BorderBrush="Gainsboro" BorderThickness="1">
                <ListBox ItemsSource="{Binding Accounts}">
                    
                </ListBox>
            </Border>
        </Grid>
        
        <!--Menu-->
        <Border Background="WhiteSmoke" BorderBrush="Gainsboro" BorderThickness="1" Grid.Column="0">
            <StackPanel>
                <Border Background="LightGray" BorderBrush="Gray" BorderThickness="1">
                    <TextBlock FontSize="30">Start</TextBlock>
                </Border>
                <Border Background="LightGray" BorderBrush="Gray" BorderThickness="1">
                    <TextBlock FontSize="30">Accounts</TextBlock>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</Window>